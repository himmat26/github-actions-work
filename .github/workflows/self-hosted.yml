name: self-hosted
on: 
  workflow_dispatch:

jobs:
  ubuntu-latest:
    runs-on: hawk
    steps:
      - name: Print system name for Ubuntu
        run: |
          echo "OS name: $RUNNER_OS"
          echo "Runner Hostname: $(hostname)"
          echo "User: $(whoami)"  
          pwd
          touch testfile
